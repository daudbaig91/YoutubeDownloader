<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.youtubedownloader">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YoutubeDownloader"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
        tools:ignore="ScopedStorage" />
    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="${Key_Google}"/>
    <meta-data
        android:name="com.google.android.geo.key_azure_url"
        android:value="${key_azure_url}"/>
    <meta-data
        android:name="com.google.android.geo.key_azure_mp3"
        android:value="${key_azure_mp3}"/>
    <meta-data
        android:name="com.google.android.geo.key_azure_mp4H"
        android:value="${key_azure_mp4H}"/>
    <meta-data
        android:name="com.google.android.geo.key_azure_mp4L"
        android:value="${key_azure_mp4L}"/>
</manifest>